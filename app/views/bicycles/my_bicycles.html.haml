.page-header
  = link_to new_bicycle_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Bicycle
  %h3 Listing bicycles

%ul.list-group
  - @bicycles.each do |bicycle|
    %li.list-group-item.row
      .col-md-2
        = link_to bicycle.name, bicycle_path(bicycle)
      .col-md-8
        = bicycle.description
      .col-md-2
        .btn-group.pull-right
          = link_to edit_bicycle_path(bicycle), class: "btn btn-primary" do
            %i.glyphicon.glyphicon-pencil
          = link_to bicycle, data: { confirm: 'Are you sure?' }, method: :delete, class: "btn btn-danger" do
            %i.glyphicon.glyphicon-trash
